name: Multi-Platform Native Build

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  build:
    name: Build Tekst and TK on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build Project
        run: cmake --build build --config Release

      - name: Upload Linux Tekst
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: Tekst-linux
          path: build/tekst
          if-no-files-found: error

      - name: Upload Linux TK
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: TK-linux
          path: build/tk
          if-no-files-found: error

      - name: Upload macOS Tekst
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: Tekst-macos
          path: build/tekst
          if-no-files-found: error

      - name: Upload macOS TK
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: TK-macos
          path: build/tk
          if-no-files-found: error

      - name: Upload Windows Tekst
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: Tekst-windows
          path: build/Release/tekst.exe
          if-no-files-found: error

      - name: Upload Windows TK
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: TK-windows
          path: build/Release/tk.exe
          if-no-files-found: error
